主分区：最多只能有4个
拓展分区：
    最多只能有1个。
    主分区加拓展分区最多有4个。
    不能写入数据，只能包含逻辑分区。
逻辑分区



4、挂载
  /（根分区）
  swap分区（交换分区，内存两倍，不超过2GB）

推荐分区
  /boot(启动分区，200M)




                      总结
  1.分区：把大硬盘分为小的逻辑分区。
  2.格式化：写入文件系统。
  3.分区设备文件名：给每个分区定义设备文件名。
  4.挂载：给每个分区分配挂载点。
